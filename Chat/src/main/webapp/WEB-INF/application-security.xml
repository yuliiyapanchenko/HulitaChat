<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns="http://www.springframework.org/schema/security"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:import resource="application-persistence.xml"/>
    <beans:import resource="application-spring.xml"/>

    <http pattern="/resources/**" security="none"/>

    <http auto-config="true" use-expressions="true">
        <!-- Login pages -->
        <form-login login-page="/login" authentication-failure-url="/login-error"
                    login-processing-url="/j_spring_security_check" username-parameter="j_username"
                    password-parameter="j_password" default-target-url="/main"/>
        <logout logout-success-url="/login" logout-url="/j_spring_security_logout"/>

        <!-- Anyone can access these urls -->
        <intercept-url pattern="/auth/**" access="permitAll()"/>
        <intercept-url pattern="/login" access="permitAll()"/>
        <intercept-url pattern="/j_spring_security_logout" access="permitAll()"/>
        <intercept-url pattern="/login-error" access="permitAll()"/>
        <intercept-url pattern="/signin/**" access="permitAll()"/>
        <intercept-url pattern="/signup/**" access="permitAll()"/>
        <intercept-url pattern="/user/register/**" access="permitAll()"/>

        <!-- Security zones -->
        <intercept-url pattern="/**" access="hasRole('ROLE_DEVELOPERS') || hasRole('ROLE_USER')"/>

        <custom-filter ref="socialAuthenticationFilter" before="PRE_AUTH_FILTER"/>

    </http>

    <authentication-manager alias="authenticationManager">

        <authentication-provider ref="socialAuthenticationProvider"/>

        <authentication-provider user-service-ref="daoUserService">
            <password-encoder hash="bcrypt"/>
        </authentication-provider>

        <!--LDAP auth -->
        <ldap-authentication-provider server-ref="contextSource"
                                      user-context-mapper-ref="ldapUserDetailsMapper"
                                      user-search-filter="(uid={0})"
                                      user-search-base="ou=people"
                                      group-search-filter="(uniqueMember={0})"
                                      group-search-base="ou=groups"
                                      group-role-attribute="cn"
                                      role-prefix="ROLE_"/>

    </authentication-manager>

    <ldap-server ldif="classpath:test-server.ldif" id="contextSource"/>

    <!--TODO: setup ldap server and add its config to app props-->
    <!--<ldap:context-source id="contextSource"-->
    <!--password="${sample.ldap.password}"-->
    <!--url="${sample.ldap.url}"-->
    <!--username="${sample.ldap.userDn}"-->
    <!--base="${sample.ldap.base}" />-->

    <beans:bean id="ldapUserDetailsMapper"
                class="com.jpanchenko.chat.dto.security.ChatLdapUserDetailsMapper"/>

    <beans:bean id="socialAuthenticationProvider"
                class="org.springframework.social.security.SocialAuthenticationProvider">
        <beans:constructor-arg index="0" ref="usersConnectionRepository"/>
        <beans:constructor-arg index="1" ref="chatSocialUserDetailsService"/>
    </beans:bean>

    <beans:bean id="chatSocialUserDetailsService" class="com.jpanchenko.chat.service.security.ChatSocialUserDetailsService"/>

    <beans:bean id="socialAuthenticationFilter" class="org.springframework.social.security.SocialAuthenticationFilter">
        <beans:constructor-arg index="0" ref="authenticationManager"/>
        <beans:constructor-arg index="1" ref="userIdSource"/>
        <beans:constructor-arg index="2" ref="usersConnectionRepository"/>
        <beans:constructor-arg index="3" ref="connectionFactoryLocator"/>
        <!-- Sets the url of the registration form. -->
        <beans:property name="signupUrl" value="/user/register"/>
    </beans:bean>

    <beans:bean id="userIdSource" class="org.springframework.social.security.AuthenticationNameUserIdSource"/>

    <beans:bean id="textEncryptor" class="org.springframework.security.crypto.encrypt.Encryptors"
                factory-method="noOpText"/>

</beans:beans>
